<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.domi.udonsari.mapper.ProfileMapper">

    <!-- 선택한 유저의 프로필 조회 -->
    <select id="selectProfileInfo" parameterType="kr.domi.udonsari.vo.profileVO" resultType="kr.domi.udonsari.vo.profileVO">
        select * from USER;
    </select>

</mapper>